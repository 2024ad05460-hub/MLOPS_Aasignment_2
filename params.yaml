data:
  raw_dir: data/raw
  processed_dir: data/processed
  image_size: 224
  train_ratio: 0.80
  validation_ratio: 0.10
  seed: 42
  max_per_class: null
training:
  architecture: tinycnn
  pretrained: false
  freeze_backbone: false
  epochs: 5
  batch_size: 16
  learning_rate: 0.001
  weight_decay: 0.0001
  seed: 42
